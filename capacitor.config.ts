import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.warllemedicao.meufinanceiro',
  appName: 'Meu Financeiro',
  webDir: 'dist'
};

export default config;
